const num1 = 5;
const num2 = 10;

function addValues() {
  console.log(`the sum is : ${num1 + num2}`);
}

addValues(); // inside the module we are invoking the function so, no need to store it in any variable wherever we require this file
